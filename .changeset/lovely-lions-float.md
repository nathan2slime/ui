---
"@nathan3boss/ui": patch
---

Harden system theme detection and media query subscriptions in `UiThemeProvider`, including compatibility fallbacks for environments without `matchMedia` and older `MediaQueryList` listener APIs.
