# Function: ThemeProvider()

> **ThemeProvider**(`__namedParameters`): `Element`

Provides a styled-components theme, using the library default when no
explicit theme is supplied.

## Parameters

### \_\_namedParameters

[`ThemeProviderProps`](../types/theme.ThemeProviderProps.md)

## Returns

`Element`

## Example

```tsx
<ThemeProvider>
  <App />
</ThemeProvider>
```
