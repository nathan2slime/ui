# Function: Toast()

> **Toast**(`props`): `Element`

Renders one toast item from a Zag toast group.

## Parameters

### props

[`ToastProps`](../types/components_toast.ToastProps.md)

Toast data, parent group service, and index supplied by `Toaster`.

## Returns

`Element`

The accessible toast item.
