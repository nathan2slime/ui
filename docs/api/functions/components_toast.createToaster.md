# Function: createToaster()

> **createToaster**(`props?`): [`ToastStore`](../types/components_toast.ToastStore.md)

Creates an isolated toast store for rendering with `Toaster`.

## Parameters

### props?

`ToastStoreProps`

Store placement, overlap, duration, and queue options.

## Returns

[`ToastStore`](../types/components_toast.ToastStore.md)

A Zag toast store that can create, update, and dismiss toasts.
