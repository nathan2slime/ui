# Type Alias: SelectDefaultProps

> **SelectDefaultProps** = [`SelectBaseProps`](components_select.SelectBaseProps.md)\<[`SelectOption`](components_select.SelectOption.md)\> & `Partial`\<\{ `isItemDisabled`: (`item`) => `boolean`; `itemToString`: (`item`) => `string`; `itemToValue`: (`item`) => `string`; \}\>

Props accepted for the built-in `{ label, value, disabled? }` item shape.
