# Type Alias: ToasterProps

> **ToasterProps** = `Partial`\<\{ `className`: `string`; `label`: `string`; `store`: [`ToastStore`](components_toast.ToastStore.md); \}\>

Props accepted by the toast region component.
